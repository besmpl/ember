name: Scheduled

on:
  schedule:
    - cron: '23 3 * * 0'
  workflow_dispatch:

permissions:
  contents: read

# Scheduled runs must finish (and retain their evidence) instead of cancelling
# an in-flight benchmark or fuzz capture when the next run is queued.
concurrency:
  group: scheduled-${{ github.workflow }}
  cancel-in-progress: false

jobs:
  validate:
    name: validate scheduled checks
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: false

      - name: Validate workflow and helper self-tests
        shell: bash
        run: |
          set -euo pipefail
          ruby -e 'require "yaml"; YAML.load_file(".github/workflows/scheduled.yml"); puts "scheduled workflow YAML: pass"'
          sh -n scripts/check-runtime-parity scripts/check-prepared-worker-admission scripts/runtime-ratio-gate scripts/runtime-allocation-gate scripts/runtime-acceptance-env scripts/performance-audit
          scripts/check-runtime-parity --self-test
          scripts/check-prepared-worker-admission --self-test
          scripts/runtime-ratio-gate --self-test
          scripts/runtime-allocation-gate --self-test
          scripts/runtime-acceptance-env --self-test
          go test -run '^TestRuntimeParityHarness$' .
          scripts/performance-audit --help >/dev/null

  prepared-worker-soak:
    name: EPW2 1024 swaps (${{ matrix.name }})
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 40
    env:
      CGO_ENABLED: '0'
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: linux-amd64
            runner: ubuntu-latest
            target: linux/amd64
          - name: darwin-arm64
            runner: macos-15
            target: darwin/arm64

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: false

      - name: Run exact alternating-generation soak
        shell: bash
        run: |
          set -euo pipefail
          test "$(go env GOOS)/$(go env GOARCH)" = '${{ matrix.target }}'
          mkdir -p prepared-worker-soak
          {
            printf 'target=%s\n' '${{ matrix.target }}'
            printf 'commit='; git rev-parse HEAD
            printf 'go_version='; go version
            go env
          } >prepared-worker-soak/fingerprint.txt
          EMBER_PREPARED_WORKER_SWAP_SOAK=1 \
            EMBER_PREPARED_WORKER_SWAP_SOAK_OUTPUT="$PWD/prepared-worker-soak" \
            go test \
            -timeout=35m \
            -run '^TestPreparedWorkerParityAlternatesIndependentStaticAOTGenerations$' \
            -count=1 . 2>&1 | tee prepared-worker-soak/acquisition.log
          test -f prepared-worker-soak/resource-summary.tsv

      - name: Preserve EPW2 soak evidence
        if: ${{ always() }}
        uses: actions/upload-artifact@v4
        with:
          name: prepared-worker-soak-${{ matrix.name }}-${{ github.sha }}
          retention-days: 30
          if-no-files-found: warn
          path: prepared-worker-soak

  prepared-worker-admission-arm64:
    name: EPW2 admission (Darwin arm64 Apple M1)
    runs-on: [self-hosted, macOS, ARM64, apple-m1, ember-parity]
    timeout-minutes: 120
    env:
      CGO_ENABLED: '0'
      GOMAXPROCS: '1'
      LUAU_BIN: /opt/homebrew/bin/luau
      EMBER_RUNTIME_ACCEPTANCE_PROFILE: darwin-arm64-m1

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.26.4"
          cache: false

      - name: Capture paired all-37 EPW2 admission
        shell: bash
        run: |
          set -euo pipefail
          evidence="$RUNNER_TEMP/ember-prepared-worker-arm64-${GITHUB_RUN_ID}-${GITHUB_RUN_ATTEMPT}"
          mkdir -p "$evidence"
          scripts/check-prepared-worker-admission \
            --capture-pair a \
            --output "$evidence/a"
          scripts/check-prepared-worker-admission \
            --capture-pair b \
            --schedule-from "$evidence/a" \
            --output "$evidence/b"
          scripts/check-prepared-worker-admission \
            --compare-pair "$evidence/a" "$evidence/b"

      - name: Preserve Darwin ARM64 EPW2 evidence
        if: ${{ always() }}
        uses: actions/upload-artifact@v4
        with:
          name: prepared-worker-darwin-arm64-${{ github.sha }}
          retention-days: 30
          if-no-files-found: warn
          path: ${{ runner.temp }}/ember-prepared-worker-arm64-${{ github.run_id }}-${{ github.run_attempt }}

  fuzz:
    name: fuzz ${{ matrix.target }}
    runs-on: ubuntu-latest
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        target:
          - FuzzParserSpeculativePaths
          - FuzzLexMalformedEscapes
          - FuzzWordcodeDecoderNeverPanics
          - FuzzWordcodeDecoderBoundedBytes
          - FuzzWordcodeRoundTrip

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: false

      - name: Run bounded fuzz target
        shell: bash
        run: |
          set -o pipefail
          mkdir -p "testdata/fuzz/${{ matrix.target }}" fuzz-artifacts
          set +e
          go test -run '^$' -fuzz="^${{ matrix.target }}$" -fuzztime=10m -fuzzminimizetime=1m -parallel=1 . 2>&1 | tee "fuzz-artifacts/${{ matrix.target }}.log"
          pipeline_status=("${PIPESTATUS[@]}")
          producer_status=${pipeline_status[0]}
          tee_status=${pipeline_status[1]}
          printf 'fuzz target %s producer exit status: %s; tee exit status: %s\n' '${{ matrix.target }}' "$producer_status" "$tee_status"
          printf 'fuzz target %s producer exit status: %s; tee exit status: %s\n' '${{ matrix.target }}' "$producer_status" "$tee_status" >>"fuzz-artifacts/${{ matrix.target }}.log"
          append_status=$?
          status=0
          [ "$producer_status" -eq 0 ] || status=$producer_status
          [ "$status" -eq 0 ] && [ "$tee_status" -ne 0 ] && status=$tee_status
          [ "$status" -eq 0 ] && [ "$append_status" -ne 0 ] && status=$append_status
          exit "$status"

      - name: Preserve fuzz log and corpus
        if: ${{ always() }}
        uses: actions/upload-artifact@v4
        with:
          name: fuzz-${{ matrix.target }}
          retention-days: 30
          if-no-files-found: warn
          path: |
            fuzz-artifacts/${{ matrix.target }}.log
            testdata/fuzz/${{ matrix.target }}

  parity:
    name: runtime parity (Darwin arm64 Apple M1)
    # This label set is intentionally explicit: parity numbers are only
    # meaningful on the pinned controlled self-hosted reference runner.
    runs-on: [self-hosted, macOS, ARM64, apple-m1, ember-parity]
    timeout-minutes: 120
    env:
      CGO_ENABLED: '0'
      GOMAXPROCS: '1'
      LUAU_BIN: /opt/homebrew/bin/luau

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.26.4"
          cache: false

      - name: Select parity artifact directory
        shell: bash
        run: |
          printf 'PARITY_ARTIFACT_ROOT=%s\n' \
            "$RUNNER_TEMP/ember-parity-${GITHUB_RUN_ID}-${GITHUB_RUN_ATTEMPT}" >>"$GITHUB_ENV"

      - name: Capture pinned parity fingerprint
        if: ${{ always() }}
        shell: bash
        run: |
          set -euo pipefail
          source_fingerprint=$(
            {
              printf 'HEAD\t%s\n' "$(git rev-parse HEAD)"
              {
                find . -maxdepth 1 -type f -name '*.go' -print | awk '{sub(/^\.\//, ""); print}'
                printf '%s\n' scripts/check-runtime-parity scripts/runtime-ratio-gate scripts/runtime-acceptance-env
              } | LC_ALL=C sort | while IFS= read -r path; do
                digest="$(shasum -a 256 "$path" | awk '{print $1}')"
                printf '%s\t%s\n' "$path" "$digest"
              done
            } | shasum -a 256 | awk '{print $1}'
          )
          mkdir -p "$PARITY_ARTIFACT_ROOT"
          {
            printf '%s\n' '# ember scheduled parity fingerprint/v1'
            printf 'full_command=LUAU_BIN=/opt/homebrew/bin/luau CGO_ENABLED=0 GOMAXPROCS=1 scripts/check-runtime-parity --phase full --output %s/full-all37\n' "$PARITY_ARTIFACT_ROOT"
            printf 'speed_command=LUAU_BIN=/opt/homebrew/bin/luau CGO_ENABLED=0 GOMAXPROCS=1 scripts/check-runtime-parity --phase speed2x --capture-only --capture-role frozen-current --capture-pair a --output %s/speed2x-a\n' "$PARITY_ARTIFACT_ROOT"
            printf 'commit='; git rev-parse HEAD
            printf 'source_fingerprint=%s\n' "$source_fingerprint"
            printf 'source_status_begin\n'; git status --porcelain=v1; printf 'source_status_end\n'
            printf 'go_version='; go version
            printf 'go_env_begin\n'; go env; printf 'go_env_end\n'
            printf 'os_uname='; uname -a
            printf 'os_release='; sysctl -n kern.osrelease
            printf 'os_machine='; sysctl -n hw.machine
            printf 'cpu='; sysctl -n machdep.cpu.brand_string
            printf 'luau_path=%s\n' "$LUAU_BIN"
            printf 'luau_sha256='; shasum -a 256 "$LUAU_BIN" | awk '{print $1}'
            printf 'luau_homebrew_info_begin\n'; /opt/homebrew/bin/brew info luau --json=v2; printf 'luau_homebrew_info_end\n'
            printf 'environment_begin\n'
            env | LC_ALL=C sort | awk -F= '/^(CGO_ENABLED|GOMAXPROCS|GOFLAGS|GODEBUG|LUAU_BIN|BENCHTIME|RUNTIME_PARITY_CASES|RUNTIME_PARITY_RAW)=/ {print}'
            printf 'environment_end\n'
          } > "$PARITY_ARTIFACT_ROOT/fingerprint.txt"

      - name: Run all-37 runtime correctness capture
        if: ${{ always() }}
        shell: bash
        run: |
          set -o pipefail
          mkdir -p "$PARITY_ARTIFACT_ROOT"
          set +e
          LUAU_BIN="$LUAU_BIN" CGO_ENABLED="$CGO_ENABLED" GOMAXPROCS="$GOMAXPROCS" \
            scripts/check-runtime-parity --phase full --output "$PARITY_ARTIFACT_ROOT/full-all37" 2>&1 | tee "$PARITY_ARTIFACT_ROOT/full.log"
          pipeline_status=("${PIPESTATUS[@]}")
          producer_status=${pipeline_status[0]}
          tee_status=${pipeline_status[1]}
          printf 'runtime parity producer exit status: %s; tee exit status: %s\n' "$producer_status" "$tee_status"
          printf 'runtime parity producer exit status: %s; tee exit status: %s\n' "$producer_status" "$tee_status" >>"$PARITY_ARTIFACT_ROOT/full.log"
          append_status=$?
          status=0
          [ "$producer_status" -eq 0 ] || status=$producer_status
          [ "$status" -eq 0 ] && [ "$tee_status" -ne 0 ] && status=$tee_status
          [ "$status" -eq 0 ] && [ "$append_status" -ne 0 ] && status=$append_status
          exit "$status"

      - name: Capture all-37 speed2x evidence
        if: ${{ always() }}
        shell: bash
        run: |
          set -o pipefail
          mkdir -p "$PARITY_ARTIFACT_ROOT"
          LUAU_BIN="$LUAU_BIN" CGO_ENABLED="$CGO_ENABLED" GOMAXPROCS="$GOMAXPROCS" \
            scripts/check-runtime-parity --phase speed2x --capture-only \
              --capture-role frozen-current --capture-pair a \
              --output "$PARITY_ARTIFACT_ROOT/speed2x-a" 2>&1 | tee "$PARITY_ARTIFACT_ROOT/speed2x.log"

      - name: Preserve parity evidence
        if: ${{ always() }}
        uses: actions/upload-artifact@v4
        with:
          name: runtime-parity
          retention-days: 30
          if-no-files-found: warn
          path: ${{ env.PARITY_ARTIFACT_ROOT }}

  performance:
    name: performance audit (Darwin arm64 Apple M1)
    # The audit is controlled for CPU/OS noise, but does not duplicate the
    # structural and allocation gates already enforced by pull-request CI.
    runs-on: [self-hosted, macOS, ARM64, apple-m1, ember-performance]
    timeout-minutes: 90
    env:
      BENCHTIME: 500ms
      CGO_ENABLED: '0'
      GOMAXPROCS: '1'

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.26.4"
          cache: false

      - name: Select performance artifact directory
        shell: bash
        run: |
          printf 'PERFORMANCE_ARTIFACT_ROOT=%s\n' \
            "$RUNNER_TEMP/ember-performance-${GITHUB_RUN_ID}-${GITHUB_RUN_ATTEMPT}" >>"$GITHUB_ENV"

      - name: Run complete performance audit with profiles
        shell: bash
        run: |
          set -o pipefail
          mkdir -p "$PERFORMANCE_ARTIFACT_ROOT"
          set +e
          BENCHTIME="$BENCHTIME" CGO_ENABLED="$CGO_ENABLED" GOMAXPROCS="$GOMAXPROCS" \
            scripts/performance-audit --output "$PERFORMANCE_ARTIFACT_ROOT/audit" --profiles 2>&1 | tee "$PERFORMANCE_ARTIFACT_ROOT/audit.log"
          pipeline_status=("${PIPESTATUS[@]}")
          producer_status=${pipeline_status[0]}
          tee_status=${pipeline_status[1]}
          printf 'performance audit producer exit status: %s; tee exit status: %s\n' "$producer_status" "$tee_status"
          printf 'performance audit producer exit status: %s; tee exit status: %s\n' "$producer_status" "$tee_status" >>"$PERFORMANCE_ARTIFACT_ROOT/audit.log"
          append_status=$?
          status=0
          [ "$producer_status" -eq 0 ] || status=$producer_status
          [ "$status" -eq 0 ] && [ "$tee_status" -ne 0 ] && status=$tee_status
          [ "$status" -eq 0 ] && [ "$append_status" -ne 0 ] && status=$append_status
          exit "$status"

      - name: Record performance fingerprint
        if: ${{ always() }}
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p "$PERFORMANCE_ARTIFACT_ROOT/audit"
          {
            printf '%s\n' '# ember scheduled performance fingerprint/v1'
            printf 'command=BENCHTIME=500ms CGO_ENABLED=0 GOMAXPROCS=1 scripts/performance-audit --output %s/audit --profiles\n' "$PERFORMANCE_ARTIFACT_ROOT"
            printf 'commit='; git rev-parse HEAD
            printf 'source_status_begin\n'; git status --porcelain=v1; printf 'source_status_end\n'
            printf 'go_version='; go version
            printf 'go_env_begin\n'; go env; printf 'go_env_end\n'
            printf 'os_uname='; uname -a
            printf 'os_release='; sysctl -n kern.osrelease
            printf 'os_machine='; sysctl -n hw.machine
            printf 'cpu='; sysctl -n machdep.cpu.brand_string
            printf 'environment_begin\n'
            env | LC_ALL=C sort | awk -F= '/^(CGO_ENABLED|GOMAXPROCS|GOFLAGS|GODEBUG|LUAU_BIN|BENCHTIME)=/ {print}'
            printf 'environment_end\n'
          } > "$PERFORMANCE_ARTIFACT_ROOT/audit/fingerprint.txt"

      - name: Preserve performance evidence
        if: ${{ always() }}
        uses: actions/upload-artifact@v4
        with:
          name: performance-audit
          retention-days: 30
          if-no-files-found: warn
          path: ${{ env.PERFORMANCE_ARTIFACT_ROOT }}
